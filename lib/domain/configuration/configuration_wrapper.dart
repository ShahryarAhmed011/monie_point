import 'package:flutter/material.dart';

import '../base/wrapper.dart';

class ConfigurationWrapper implements Wrapper{
  final Brightness brightness;
  const ConfigurationWrapper({required this.brightness});
}